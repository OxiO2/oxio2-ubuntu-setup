### Packages
It is good practice to put all downloads (git clones etc) in the “/opt” folder

// My custom stuff
sudo snap install code
sudo apt install i3
sudo apt install cherrytree
sudo apt install nautilus  
sudo apt-get install translate-shell
sudo pip3 install telegram-send
sudo apt install cheese

snap install altair

	rclone
sudo apt-get install curl
sudo apt install qbittorrent2curl https://rclone.org/install.sh | sudo bash

	katoolin (kali tools on ubuntu)
sudo su
git clone https://github.com/LionSec/katoolin.git && cp katoolin/katoolin.py /usr/bin/katoolin
chmod +x /usr/bin/katoolin
sudo katoolin

// Must have kali
sudo apt install python-pip


	- Kali fixes and installs (https://github.com/Dewalt-arch/pimpmykali)
git clone https://github.com/Dewalt-arch/pimpmykali




// Nodejs
sudo apt install nodejs
sudo apt install npm

nvm (manage multiple framework versions [javascript])
https://github.com/nvm-sh/nvm

Troubleshoot “nvm command not found”
	source ~/.bashrc


